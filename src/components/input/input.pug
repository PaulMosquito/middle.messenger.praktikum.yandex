style
    include input.css

mixin input({ name, type="text", value })
    fieldset.input-form
        input.input-form_input(placeholder=name type=type value=value)
        legend.input-form_legend=name
