include ../svg/svg.pug

style
    include modal.css

mixin modal({ id, list=[], position="top" })
    div(id=id class=`modal modal_${position}`)
        each val in list
            a(class="modal_row" href=val.href)
                div.modal__row__icon
                    +svg({ width: "22px", height: "22px", icon: val.icon })
                div.modal__row__title=val.title
                