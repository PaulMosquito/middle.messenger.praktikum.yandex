export default `
            div.login-form
                div.login-form__wrapper
					#{Title}
					div.login-form__wrapper__inputs
						#{FirstName}
						#{SecondName}
						#{Login}
						#{Email}
						#{Phone}
						#{Password}
						#{RepeatPassword}
					#{Auth}
					#{SignIn}
`